# Exact versions the reported metrics were produced with (Python 3.12.10).
# Pinned rather than lower-bounded: scikit-learn and XGBoost change their
# defaults and RNG behaviour between minor releases, so a range does not
# reproduce the numbers in the README.

# --- core ---
pandas==2.3.3
numpy==2.5.2
scikit-learn==1.9.0
xgboost==3.4.0
imbalanced-learn==0.14.2
joblib==1.5.3

# --- experiment tracking ---
mlflow==3.15.1

# --- API service ---
fastapi==0.141.1
uvicorn==0.52.1
pydantic==2.13.4

# --- figures and notebooks ---
matplotlib==3.11.1
seaborn==0.13.2
jupyter==1.1.1

# --- tests ---
pytest==9.1.1
httpx==0.28.1
